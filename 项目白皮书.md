

Atomo 平台白皮书 (v0.1)
《Atomo: 下一代分布式应用操作系统的白皮书》
摘要 (Abstract)
Atomo 旨在构建一个以 Rust 为基石、以“本地优先”和“实时协作”为核心哲学的开源平台，成为下一代分布式应用的操作系统。面对当前应用开发中普遍存在的数据孤岛、在线优先的脆弱性、以及跨平台开发的诸多妥协，Atomo 提出了一套整合的解决方案。通过其核心的事件溯源架构、客户端同步 SDK 和声明式的开发范式，Atomo 致力于将开发者从繁琐的样板代码中解放出来，赋予他们构建出真正有韧性、协作优先、且体验卓越的跨平台应用的能力。
第一章：愿景与哲学 (The Vision & Philosophy)
1.1 从 CMS 到 Content Core：心智的升维
传统的 CMS 是一个尽职尽责的“仓库管理员”，被动地接收、保管和按单取货。Atomo 则是一个全新的品类：Content Core (cc)。它不再是仓库，而是驱动整套业务体系的“方舟反应堆”——是业务的能量核心与控制中枢。它不仅仅存储内容，它用内容驱动一切，从“内容的保管者”转变为“业务的驱动者”。
1.2 核心隐喻：Atomo 的灵魂
“钢铁侠的心脏” (The Arc Reactor): Atomo 不仅提供极致性能，更象征着无与伦比的可靠性。其基于 Rust 的内存安全与并发能力，以及事件溯源的审计追溯能力，使其成为业务系统稳定运行的能量核心。
“事件的河流” (The River of Events): Atomo 的世界观里，内容不是静止的数据快照，而是一条奔腾不息的事件长河。每一次变更都是一个不可磨灭的事件原子，汇入时间。这带来了终极的版本控制、业务的时间机器能力，以及无可比拟的业务确定性。
“流动的画布” (The Flowing Canvas): Atomo 的创作体验摆脱了固化表单的束缚。它提供了一个类似 Notion 的画布，让创作者像玩乐高一样自由组合内容“原子”。AI 是画笔，开发者通过 WASM UI 插件创造新“原子”，将创作从“填写”升维为“共创”。
1.3 Atomo 的三大支柱哲学
本地优先 (Local-First): 我们坚信，应用的“真理之源”应该在用户的设备上。这不仅赋予了应用在任何网络环境下都能流畅运行的韧性，更是将数据主权交还给了用户。云端是同步与协作的中心，而非性能的瓶颈。
实时协作 (Real-time Collaboration): 人与人的连接应该被视为应用的一等公民，而非附加功能。通过底层的 CRDTs 技术，Atomo 使构建细粒度、无冲突、Figma 级的多人协作体验变得前所未有的简单。
声明式开发 (Declarative Development): 开发者的宝贵时间应该用于思考业务逻辑，而非编写重复的样板代码。通过一系列精心设计的 TypeScript DSL，开发者只需“声明”他们的数据模型、业务规则和工作流程，Atomo 的自动化工具链会负责将其转化为高性能、类型安全的后端服务和前端组件。
第二章：技术架构全景 (The Architectural Landscape)
2.1 统一技术栈：Rust 的力量
Atomo 的核心后端、CLI、同步引擎和 WASM 插件运行时均采用 Rust 编写，以获得内存安全、极致性能、可靠并发和创建一个统一、可维护的技术生态。
2.2 Atomo Core: 事件溯源与“即时编译的服务运行时”
Atomo Core 采用事件溯源 (Event Sourcing) 与 CQRS 架构。所有状态变更都以不可变事件的形式追加写入日志。atomo dev CLI 采用创新的“即时编译的服务运行时”模式：它会读取服务的 schema.ts，在临时工作区中自动生成专门的 Rust 代码（模型、解析器），并与通用的 atomo_server 库一起，即时编译成一个专属于该服务的、独立的二进制文件。这完美地分离了平台与应用的关注点，并充分利用了 Cargo 的增量编译能力。
2.3 Atomo Sync SDK: 客户端的“微型 Atomo 引擎”
这是实现“本地优先”和“实时协作”的关键。该 SDK 将被编译到各个原生平台（通过 Rust FFI, JSI）和 Web (通过 WASM)。
嵌入式数据库: 使用 SQLite 在设备端持久化数据。
CRDTs 引擎: 使用 yrs (y-crdt 的 Rust 实现) 自动处理数据变更的无冲突合并。
后台同步引擎: 在后台线程中负责与 Atomo Cloud 进行高效、可靠的数据同步。
2.4 Atomo Hydra UI: 跨平台 UI 的终极范式
为解决跨平台 UI 开发的核心困境，Atomo 提出 Hydra UI 架构：
UI 逻辑核心: 使用 Rust 或 AssemblyScript 编写与平台无关的、纯粹的 UI 状态机逻辑（数据获取、状态管理），并将其编译为 WASM 模块。
平台原生渲染器: 为每个平台（Web-React, iOS-SwiftUI, Android-Compose）编写一个极薄的渲染层，它的唯一职责就是订阅 WASM 核心的状态变化，并将其渲染成平台原生的 UI 组件。
这实现了业务逻辑的 100% 复用，同时保证了各个平台的顶级原生性能与体验。
2.5 声明式 DSL 的力量
schema.ts: 定义数据模型、关系，并集成了类型安全的 Hooks 和 Access Control DSL，用于实现细粒度的业务规则和权限控制。
workflow.ts: 一个类型安全的 TypeScript DSL，用于替代脆弱的 YAML，编排长时间运行的、异步的、跨服务的业务流程。
2.6 设计令牌系统：跨平台样式的“真理之源”
通过一个与平台无关的 tokens.json 文件来定义所有设计属性（颜色、间距、字体等）。Atomo 的构建引擎会自动将这些令牌转换为各个平台（CSS 变量, Tailwind 配置, Swift 枚举, Android XML）的原生格式，确保视觉一致性，并极大简化主题化和白标应用的开发。
第三章：旗舰应用与解决方案 (The Flagship & Solutions)
3.1 旗舰应用驱动开发：以 Atomo CRM 为例
Atomo 的核心平台能力将通过构建一个功能完备、协作优先的 CRM SaaS (Atomo CRM) 来进行验证和驱动。CRM 提出的真实、复杂的需求（如客户活动时间线、销售流程自动化）将直接指导 Atomo Core 和 SDK 的功能迭代。
3.2 目标场景与解决方案模板
最终，Atomo CRM 本身将被打包成一个“解决方案即代码”的模板。Atomo 平台的目标是催生一个解决方案市场，覆盖：
协作式内容平台 (Notion-like)
本地优先的生产力工具 (Obsidian/Logseq-like)
实时 C2C 商业平台
企业内部 LOB (Line-of-Business) 应用
第四章：开发者体验 (The Developer Experience)
4.1 atomo CLI: 自动化工作流的指挥中心
atomo CLI 是 Atomo 生态的入口和核心。它负责代码生成、即时编译、数据库迁移、前端类型生成、多服务开发环境管理等所有自动化任务，旨在为开发者提供一个无缝、心流不被打断的开发体验。
4.2 双轨开发模式：主流开发者 (TS) 与核心开发者 (Rust)
Atomo 为不同技能的开发者提供了不同的路径：
主流开发者: 通过 schema.ts 和其他 TS DSL 进行开发，享受极致的自动化和便捷。
核心开发者: 可以选择 atomo init --native-rust 模式，直接编写原生 Rust 服务，获得最极致的性能和控制力。
4.3 实时开发与调试
atomo dev 为跨语言开发提供了优化的热重载体验：
应用代码 (TS): 修改 schema.ts 会触发快速代码生成和 Rust 服务的热重启（秒级）。
平台代码 (Rust): 修改核心 Rust crate 会触发 Cargo 的增量编译和自动重启。
WASM UI 插件: 开发模式下编译到 JS 实现 HMR 和浏览器调试，生产模式下编译到 WASM。
第五章：实施路线图 (The Implementation Roadmap)
Phase 0: 核心体验原型验证 (2-3个月)
目标：验证“双模定义 (TS -> Rust)”的核心开发者体验。
产出：一个极简的、开源的 CLI 原型。


Phase 1: 奠定基石 - 打造 CRM MVP (额外 3-4个月)
目标：构建一个基于“事件友好型 CRUD”的 CRM MVP。
产出：一个可用的内部 CRM，引入持久化存储和动态 Admin UI。


Phase 2: 架构升维 - 引入同步与协作 (6-8个月)
目标：完成向事件溯源的迁移，发布 Atomo Sync SDK (v1.0) 和 Atomo Cloud (Private Beta)。
产出：支持离线可用和基础实时协作的 CRM 应用。
