# WASM Plugins

Extend your service with safe, fast WebAssembly plugins.

- Location: `services/<name>/plugins`
- Runtime: `crates/atomo_wasm_runtime`
- Use for: event handlers, content processors, integrations

Build and test with your standard workspace commands.
