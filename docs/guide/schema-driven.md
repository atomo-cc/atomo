# Schema-Driven Development

Define your domain in `schema.ts`. Atomo generates:
- Rust backend and GraphQL API
- Admin UI screens
- Client SDK types

Edit `services/<name>/schema.ts` and run `pnpm dev` in that service.
