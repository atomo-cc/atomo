# v0.7.0 release TODO

Authorized release scope: storage lifecycle and its verified query, nullable mutation, projection and admin fixes. Follow the repository Release Checklist; do not push directly to main. Update each item immediately after its evidence passes.

- [x] REL-01 DONE — local workspace check and all-target clippy passed (`data-release-check.log`, `data-release-clippy.log`); feature pushed and [PR #78](https://github.com/atomo-cc/atomo/pull/78) opened. Five-job [CI 34145166472](https://github.com/atomo-cc/atomo/actions/runs/34145166472) dispatched on `84c276b`. Existing nine browser tests are discovered with the screenshot configuration; CI retains review screenshots for seven days.
- [x] REL-02 DONE (REL-01) — all five feature jobs passed in CI 34145576654; PR #78 squash-merged as `84697f1`. All five post-merge jobs also passed in [main CI 34146051796](https://github.com/atomo-cc/atomo/actions/runs/34146051796), including real PostgreSQL and admin browser tests.

  All five jobs passed in [CI 34145576654](https://github.com/atomo-cc/atomo/actions/runs/34145576654) on `5ce07af`, including admin 9/9 in 21.1 seconds. [PR #78](https://github.com/atomo-cc/atomo/pull/78) squash-merged as `84697f1`; the feature branch was removed after verifying identical trees. [Main CI 34146051796](https://github.com/atomo-cc/atomo/actions/runs/34146051796) subsequently passed on that exact merge commit. Historical release preparation branch: `chore/release-v0.7.0`.

  First CI run: Rust/PostgreSQL, frontend and lint passed; admin smoke passed 7/9, then failed loading schema and seeding the restarted worker. Evidence retained in run 34145166472 and its screenshot artifact. The expanded suite shares one IP and repeatedly reloads the SPA against the default 100-request window; the isolated e2e server now has an explicit 1,000-request/60-second budget. Added status/path-only response diagnostics and seed HTTP status assertions; the old run did not capture HTTP statuses, so rate exhaustion is a diagnosis to verify, not a claimed observed 429. Production defaults, limiter tests and all nine UI assertions remain unchanged. The required complete rerun subsequently passed in CI 34145576654.
- [x] REL-03 DONE (REL-02) — release branch prepared; changelog dated 2026-09-08, nine Rust crates and both SDKs aligned to 0.7.0. Cargo.lock contains only the nine version changes; pnpm 8.15.0 lockfile regeneration completed with no semantic diff. Versioned workspace check/all-target clippy, both SDK prepublish builds and docs build passed (`data-release-version-*.log`, `data-release-*-build.log`). Dry-run package contents contain the expected JS/declaration entry points and no credential/temp/test artifacts: client 26 files/22,838 compressed bytes; worker 6 files/10,010 bytes. README, guide and both roadmaps now describe v0.7.0; historical evidence remains explicit.
- [x] REL-04 DONE (REL-03) — all five version CI jobs passed in [34146643279](https://github.com/atomo-cc/atomo/actions/runs/34146643279) on `15e38e0`. PR #79 squash-merged as `baadeea41dfff162d3a9b9f132e9ffc0a6678d9c`; its committed tree exactly matches the tested head. Tag v0.7.0 was pushed from merged main and [GitHub Release](https://github.com/atomo-cc/atomo/releases/tag/v0.7.0) published at 2026-09-07T17:18:50Z. The release branch was deleted after tree verification. Notes explicitly say npm, Docker and hosted docs are pending; source release is not registry publication.

  [Release PR #79](https://github.com/atomo-cc/atomo/pull/79) contains version commit `15e38e0`. [Release CI 34146643279](https://github.com/atomo-cc/atomo/actions/runs/34146643279) dispatched on that exact head; this gate subsequently passed before the source tag was published, as recorded in REL-04.
- [ ] REL-05 BLOCKED (REL-04; only public documentation access remains) — both npm SDKs and the server image have been published and verified; Docker 34171568872 and docs 34171571157 succeeded. REL-05N/REL-05D are DONE. The deployed docs artifact contains the v0.7.0/history/audit guide, but external domain DNS/Pages certificate failure prevents public access; REL-05W remains BLOCKED. This is not a package authentication or build failure, and published artifacts must not be republished to resolve it.
- [x] REL-06 DONE (REL-05D; independent of external docs DNS) — authorized dependent deployment now reports v0.7.0/baadeea41 from the published image. Existing world/season and 14 accounts preserved; history/audit model policies match the previous verified configuration. Six application partitions and the maintenance worker are healthy (apply enabled, zero errors). Real desktop/mobile browser entry checks and strict capacity audit passed. Following transient restart backoff, five samples over 60 seconds showed sequence 17575→17599, cursor 17561→17598, lag 1–16, circuit closed and zero consecutive failures. This verifies the version upgrade; public docs availability remains separately blocked in REL-05W.

- [x] REL-05N DONE (REL-04; split publication evidence) — npm login and required interactive publication verification completed. Both client-sdk and worker-sdk 0.7.0 published successfully; registry versions and full SHA512 integrity match the pre-release pack dry-runs (`.tmp/client-published.json`, `.tmp/worker-published.json`). No source/package changes were made after the tested tag.
- [x] REL-05D DONE (REL-05N) — [Docker workflow 34171568872](https://github.com/atomo-cc/atomo/actions/runs/34171568872) succeeded. Image v0.7.0 pulled successfully; it and latest share index digest `sha256:d00e911046c1f908c406399a4cc9ac94d3a1c2eaf231285f4d83dd1421436e4a`. Image configuration reports version v0.7.0, revision baadeea41dfff162d3a9b9f132e9ffc0a6678d9c and build time 2026-09-07T23:54:32Z, matching the source tag.
- [ ] REL-05W BLOCKED (REL-05N) — docs build/deploy run 34171571157 succeeded from baadeea41, but the public guide cannot resolve. Public DNS returns SERVFAIL and authoritative servers return REFUSED for docs.atomo.cc and the zone; Pages reports certificate state bad_authz. Evidence: `.tmp/docs-dns-failure.json`. Requires restoration of zone/NS delegation in the domain/DNS account, then valid HTTPS and the live guide containing v0.7.0/history/audit configuration. The GitHub Pages fallback redirects to the same failed custom domain. Do not call the hosted docs available or silently change the public domain. Image publication and its dependent runtime verification can continue independently.

CLI source is unchanged from v0.6.5: optional CLI binary publication is skipped unless release preparation finds a relevant CLI change. crates.io publishing remains disabled by repository policy. Public packages and the server image are still required.

Package scope audit: the release checklist explicitly names client-sdk and worker-sdk for synchronized publication. The unchanged create-app scaffolder has an independent 0.1.3 release line (verified in npm), and the workspace schema DSL is not publicly registered (npm 404); neither receives an incidental first publication or version-line migration in this storage release. The private admin, documentation and service packages are not npm release artifacts.

Feature implementation and local test evidence: [TODO.md](TODO.md). This checklist records publication status separately; local feature completion does not imply release completion.

Historical publication preflight: GitHub identity was available, but `npm whoami` returned 401 on 2026-09-08. Both SDKs were still 0.5.10 in npm; nine Rust crates were 0.6.4 despite the v0.6.5 GitHub tag. REL-03 aligned source versions to 0.7.0; REL-05 remains the authority for actual registry availability. Never record credentials in release evidence.
