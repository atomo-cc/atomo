# Storage lifecycle TODO

Each item is checked immediately after its acceptance evidence passes. TODO / IN_PROGRESS / REVIEW / BLOCKED remain unchecked.

- [x] AT-QU-01 DONE — one-statement set filters preserve scope through read/update/soft-delete/restore/hard-delete, projection removal, empty/malformed inputs and JSON-array writes. Evidence: SQL builder regression and `scoped_set_filters` serial PostgreSQL test both passed in `data-lifecycle-set-filters.log`; latest workspace all-target clippy and server build passed.

- [x] AT-ST-01 DONE — inspect rules and existing paths, define requirements and ownership; evidence: IMPLEMENTATION_PLAN.md and source audit of cache, core CRUD, event store, server audit listener and projector rebuild.
- [x] AT-CA-01 DONE — bounded cache configuration, expiration/reclamation and metrics; evidence: `cargo test -p atomo --lib cache::tests` passed 10/10 on 2026-09-07 (tool chunk 2c9ff4), including disabled/TTL/TTI/entry/byte bounds, weak maintenance ownership and runtime shutdown/restart. Whole-workspace validation remains AT-QA-01.
- [x] AT-CA-02 DONE — complete read keys, atomic generation barriers, request coalescing and explicit multi-instance bypass; evidence: cache unit tests 10/10 and `cargo test -p atomo --test cache_lifecycle -- --include-ignored --test-threads=1` passed 2/2 against isolated PostgreSQL (tool chunk 26e9e6). Includes bypass outer caching; actual belongsTo, tenant partition, cancelled invalidation and stale fills verified.
- [x] AT-CA-03 DONE — model enable/TTL/TTI/refresh/entry-count/byte overrides within global budgets, unknown-model rejection and coalesced demand refresh; evidence: the same 10/10 unit and 2/2 integration runs prove absolute expiry, model policy and startup validation. These previously verified results were accidentally replaced with earlier task descriptions during shared-file editing; restored from actual tool results, not inferred completion.
- [x] AT-HI-01 DONE — full/off/retained history, independent audit modes, all mutation fan-out, strict model validation before DB connection and stable gap-marker row version verified. Evidence: latest `data-lifecycle-tests.log` unknown-model regression and `data-lifecycle-pg-tests.log` core lifecycle plus HTTP mutation/audit tests passed; previous evidence retained in the implementation history.
- [x] AT-HI-02 DONE — latest `data-lifecycle-pg-tests.log` verifies replay/preflight locking, persistent gaps across mode changes, bounded pruning, single-connection atomic all-model rebuild, administrator-only GET/POST 401/403/409 guards and extreme-age arithmetic. Maintenance lifetime unit test passed in `data-lifecycle-tests.log`; notification fan-out remains best-effort, not a durable outbox. Remaining workspace lint/build gates belong to AT-QA-01.
- [x] AT-IN-01 DONE — ServerConfig/default/env, core builder and administrator diagnostics wired. Evidence: configuration compatibility unit test; `storage_lifecycle_http` verifies real router 401/403/200, persisted history-off mutation and metadata audit. Two successive full serial PG runs passed (`data-lifecycle-pg-tests.log`, `data-lifecycle-pg-repeat.log`) after fixing precise self-registration fixture cleanup.
- [x] AT-DO-01 DONE — complete repository feature documentation sweep, generic examples only.
- [x] AT-UI-01 DONE - existing audit displays use runtime policy, with off/metadata/mixed/unavailable semantics. Actual admin regression fixes wire creation permissions and an existing search field into the embedded list. Evidence: type-check +29 unit tests; full existing real-browser smoke 9/9 with default full audit (17.0s) and 9/9 with audit off (24.2s), including explicit successful positive-search GraphQL response and 503 policy fault. Durable off-run log: `data-lifecycle-admin-off.log`. No additional smoke suite created.
- [x] AT-QA-01 DONE — current workspace unit/doc tests and serial PostgreSQL tests pass, including scoped set filters; previous consecutive PG runs also prove fixture repeatability. Latest-stable all-target clippy, fmt check and server build pass. All frontend package tests, SDK builds, 29 admin unit tests, default/off real browser smoke (9 each), production admin build and documentation build pass. Logs: `data-lifecycle-tests.log`, `data-lifecycle-pg-tests.log`, `data-lifecycle-clippy.log`, `data-lifecycle-fmt.log`, `data-lifecycle-build.log`, `data-lifecycle-frontend-tests.log`, `data-lifecycle-admin-off.log`, `data-lifecycle-admin-build.log`, `data-lifecycle-sdk-build.log`. The existing Response/Response middleware uses a documented boundary-only lint allowance to preserve its public signature.
- [ ] AT-QA-02 IN_PROGRESS — final requirement audit and focused local commits; runtime cache/history/diagnostics, safe filtered permanent deletion and projection updates also passed real HTTP integration against an isolated database. No public release, push or merge is requested.

- AT-UI-01 browser baseline finding: historical smoke expected a Dashboard heading and quick-create cards removed by the current console design. Updating the same spec to current registry navigation while retaining model label, create permission, enum, timestamp and File safety assertions; default and off-policy browser acceptance both passed 9/9.
