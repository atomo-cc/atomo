# Examples

Explore end-to-end examples and patterns.

- CRM System — see `services/crm-service`
- Admin UI — see `packages/atomo-admin-ui`
- Client SDK — see `packages/atomo-client-sdk`

Start the CRM example:
```bash
cd services/crm-service && pnpm dev
```
